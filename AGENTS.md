# RealEstate Helper 개발 지침

## 언어

- 문서, 주석, 사용자에게 노출되는 문구는 원칙적으로 한국어로 작성한다.
- 코드의 변수명, 함수명, 클래스명, 타입명은 영어를 사용한다.

## Clean Code 원칙

- 모든 구현은 Clean Code 원칙을 따른다.
- 이름만 보고 역할을 이해할 수 있도록 명확한 이름을 사용한다.
- 함수와 컴포넌트는 한 가지 책임만 갖도록 작게 유지한다.
- 중복 로직은 공통 함수, 훅, 서비스 또는 도메인 모듈로 분리한다.
- 불필요한 주석보다 의도가 드러나는 코드와 타입을 우선한다.
- 매직 넘버와 하드코딩된 정책 값은 의미 있는 상수 또는 설정으로 분리한다.
- 부수 효과를 최소화하고, 순수 함수로 표현할 수 있는 도메인 로직은 순수 함수로 작성한다.
- 오류를 숨기지 않으며, 사용자 오류와 시스템 오류를 구분해 처리한다.
- 변경 이유가 서로 다른 코드는 같은 모듈에 묶지 않는다.
- 테스트하기 어려운 구조를 발견하면 기능 추가 전에 의존성과 책임을 먼저 분리한다.

## God Object / God Class 금지

- God Object와 God Class를 만들지 않는다.
- 하나의 클래스, 객체, 함수, React 컴포넌트 또는 서비스에 여러 도메인의 책임을 집중시키지 않는다.
- UI 렌더링, 자격 판정, 데이터 수집, 공고문 분석, 저장소 접근을 하나의 모듈에서 처리하지 않는다.
- 파일이 지나치게 커지거나 변경 이유가 둘 이상이면 책임별 모듈로 분리한다.
- 전역 상태에 모든 데이터를 몰아넣지 않고 기능 단위의 최소 상태만 관리한다.
- 공통 모듈이라는 이유로 관련 없는 기능을 계속 추가하지 않는다.

## 권장 책임 경계

- `domain`: 자격 조건, 공고, 주택, 제출 서류 등 핵심 모델과 규칙
- `application`: 공고 검색, 자격 판정, 서류 목록 생성 등 사용 사례
- `infrastructure`: LH·SH 및 공공데이터 API, 크롤러, 데이터베이스 연동
- `components`: 표현 중심의 재사용 가능한 UI
- `features`: 사용자 조건 입력, 공고 탐색, 공고 상세, 서류 관리 등 기능 단위 코드

## 구현 및 검증

- 외부 공고 데이터와 샘플 데이터를 명확히 구분한다.
- 자격 판정 결과에는 적용한 조건과 판정 근거를 함께 제공한다.
- 공고문 원문과 최종 모집 자격을 사용자가 다시 확인할 수 있도록 출처를 보존한다.
- 개인정보는 필요한 최소 범위만 수집하며 로그나 샘플 데이터에 포함하지 않는다.
- 기능 변경 후 관련 테스트, 타입 검사, 빌드를 수행한다.

